class basic {
   public static void main(String[] args) {

      // String str = "abcd";
      // for (int i = 0; i < str.length(); i++) {
      // System.out.println(str.substring(i));
      // }
      // System.out.println(str.substring(0, 3));
      // }
      // for (int i = 0; i < 3; i++) {

      // for (int j = i + 1; j < 4; j++) {

      // System.out.print(str.substring(i, j) + " ");
      // }
      // }

      // String str = "mukesh";
      // String gtr = "mukesh";
      // str and gtr store in a same memory ans address
      // String s = new String("mukesh");/// extra copy new string and new mempoy
      // System.out.println(str);

      
      // String str = "hello";
      // str.charAt(0)="a";/// not change h (immutablity);

   }
}